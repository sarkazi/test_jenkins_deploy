properties([disableConcurrentBuilds()])

pipeline {
  agent any

  options {
    buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
    timestamps()
  }

  stages {
    stage ('Test test etst') {
      steps {
        echo "================= тест пошел ======================"
        dir ('docker/test') {
          sh '''
            echo 'pisun'
            echo 'zhopa'
            cd ..
            cd ..
            cd ..
            ls -la
          '''
        }
      }
    }
  }
}