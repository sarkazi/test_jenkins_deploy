properties([disableConcurrentBuilds()])

pipeline {
  agent any

  options {
    buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
    timestamps()
  }

  stages {
    stage ('Build') {
      steps {
        echo "Писька"
      }
    }
    stage ('Test') {
      steps {
        echo "Жопа"
      }
    }
    stage ('Deploy') {
      steps {
        echo "Сиська"
      }
    }
  }
}