properties([disableConcurrentBuilds()])

pipeline {
  agent any

  options {
    buildDiscarder(logRotator(numToKeepStr: '10', artifactToNumStr: '10'))
    timestamps()
  }

  stages {
    stage ('First step') {
      steps {
        echo "piska"
      }
    }
  }
}